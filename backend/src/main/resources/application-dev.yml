spring:
  datasource:
    url: jdbc:h2:mem:autoproject;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    com.example.autoproject: DEBUG
